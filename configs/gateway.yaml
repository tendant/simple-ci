server:
  port: 8081
  read_timeout: 30s
  write_timeout: 30s

auth:
  api_keys:
    - name: "local-dev"
      key: "dev-key-12345"
    - name: "ci-dashboard"
      key: "dashboard-key-67890"

concourse:
  url: "http://localhost:9001"
  team: "main"
  username: "admin"
  password: "admin"
  token_refresh_margin: 5m
  # For now, use token from fly CLI (~/.flyrc)
  # Run: grep -A 2 "local:" ~/.flyrc | grep "value:" | awk '{print $2}'
  bearer_token: "Te/3FtIKdzJpCWmlE9TYQ3QRHxnrtmFpAAAAAA"

logging:
  level: "info"
  format: "json"
