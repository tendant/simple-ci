# Simple CI Gateway Configuration

# Server
SERVER_PORT=8081
SERVER_READ_TIMEOUT=30s
SERVER_WRITE_TIMEOUT=30s

# Authentication
# Comma-separated list of name:key pairs
API_KEYS=local-dev:dev-key-12345,ci-dashboard:dashboard-key-67890

# Concourse CI
CONCOURSE_URL=http://localhost:9001
CONCOURSE_TEAM=main

# Authentication: Choose ONE of the following methods:
# 1. Username/Password (recommended for dev, tokens auto-refresh)
CONCOURSE_USERNAME=admin
CONCOURSE_PASSWORD=admin

# 2. Pre-configured Bearer Token (optional, for long-lived tokens)
#    To get a token: fly -t <target> login, then: fly -t <target> userinfo --json | jq -r .token.value
# CONCOURSE_BEARER_TOKEN=

CONCOURSE_TOKEN_REFRESH_MARGIN=5m

# Logging
LOG_LEVEL=info
LOG_FORMAT=json

# Jobs Configuration
JOBS_FILE=configs/jobs.yaml
